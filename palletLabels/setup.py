from setuptools import setup, find_packages

setup(
    name='palletLabels',
    version='1.0',
    description='A program for creating barcoded labels.',
    author='Matt R',
    url='github.com/mattrrobins',
    packages=find_packages()
)
